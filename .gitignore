# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/
pubspec.lock

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/web/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Secrets & local config
env.json
assets/env.json
android/app/google-services.json
ios/Runner/GoogleService-Info.plist

# Local envs, coverage, logs and captures
coverage/
.venv/
.venv_cv/
logs/
pr_captures/
*.rar
*.zip

# Node server env (keep secrets local)
server/express/.env
**/.env

# Local tooling SDK/NDK and heavy caches (não versionar)
.tooling/

# Backups gerados pelos scripts
backups/

# Windows reserved filenames accidentally created
CON
NUL

# ==========================================
# KEYSTORE AND SIGNING (NEVER COMMIT!)
# ==========================================
# These files contain sensitive information for app signing
# If you commit these, anyone can sign apps as you!
*.jks
*.keystore
key.properties
android/key.properties
android/upload-keystore.jks
android/app/upload-keystore.jks

# Play Store credentials
play-store-credentials.json
