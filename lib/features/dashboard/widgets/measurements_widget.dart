import 'package:flutter/material.dart';
import '../../../core/widgets/nutriz_card.dart';

class MeasurementsWidget extends StatelessWidget {
  const MeasurementsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const NutrizCard(child: Text('Measurements placeholder'));
  }
}

