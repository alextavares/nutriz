name = "nutritracker-worker"
main = "./worker.js"
compatibility_date = "2024-05-01"
workers_dev = true

[vars]
# Non-secrets here (override no painel se preferir)
OPENAI_MODEL = "gpt-4o-mini"
VISION_PROVIDER = "openai"
# ALLOWED_ORIGINS e APP_TOKEN: defina no painel ou via `wrangler secret put`

[observability]
enabled = true
